@using TechNews.Models;
@using PagedList.Core.Mvc;
@using TechNews.Ultilities
@model PagedList.Core.IPagedList<Account>

@{
    Layout = "/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" clasa="main">
    <div class="pagetitle">
        <h2>Quản lý bài viết</h2>
        <div class="row">
            <div class="col-4">
                <a type="button" class="btn btn-success" asp-area="Admin" asp-controller="Account" asp-action="Create">
            <i class="bi bi-file-earmark-text me-1"></i>Tao tai khoan</a>
            </div>
            <div class="col-8">
                <div>
                    <ul class="pagination float-end">
                        <pager class="pager-container" options="@PagedListRenderOptions.Bootstrap4PageNumbersOnly" list="@Model"></pager>
                    </ul>
                </div>
            </div>
        </div>
        <section class="section dashboard">
            <div class="row">
                <div class="col-12">
                    <div class="card recent-sales overflow-auto">
                        <div class="card-body mt-4">
                            <table class="table table-borderless datatable">
                                <thead>
                                    <tr>
                                        <th class="col-1 text-center">Username</th>
                                        <th class="col-3 text-center">Name</th>
                                        <th class="col-2 text-center">Email</th>
                                        <th class="col-2 text-center">GenderId</th>
                                        <th class="col-1 text-center">RoleId</th>
                                    </tr>
                                </thead>
                                <tbody>
                                        @foreach (var item in Model)
                                        {
                                        <tr>
                                            <td class="text-center" scope="row">@item.AccountId</td>
                                            <td class="text-center" scope="row">@item.Name</td>
                                            <td class="text-center" scope="row">@item.Email</td>
                                            <td class="text-center" scope="row">@item.GenderId</td>
                                            <td class="text-center" scope="row">@item.RoleId</td>
                                            <td class="text-center" scope="row">
                                                <a asp-area="Admin" asp-controller="Account" asp-action="Edit"
                                                    asp-route-id="@item.AccountId" class="btn btn-primary btn-sm"
                                                    title="Edit the post"><i class="bi bi-pencil"></i></a>
                                                <a asp-area="Admin" asp-controller="Account" asp-action="Delete"
                                                    asp-route-id="@item.AccountId" class="btn btn-danger btn-sm"
                                                    title="Delete the post"><i class="bi bi-trash"></i></a>
                                            </td>
                                        </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </p>
    </div>
</main>